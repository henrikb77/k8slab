apiVersion: apps/v1
kind: Deployment
metadata:
  name: badserver-deployment
  labels:
    app: badserver-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: badserver-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: badserver-app
    spec:
      containers:
      - name: badserver
        image: badserver:1
